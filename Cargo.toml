[package]
name = "manto"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "MIT"
description = "A terminal-driven desktop environment written in Rust"
repository = "https://github.com/PedroMagar/manto"
readme = "README.md"
keywords = ["desktop", "window-manager", "terminal", "tui"]
categories = ["command-line-interface", "os"]

[dependencies]

[target.'cfg(unix)'.dependencies]
libc = "0.2"
