[package]
name = "manto"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "A terminal-driven desktop environment written in Rust"

[dependencies]
#serde = { version = "1.0", features = ["derive"] }
#serde_json = "1.0"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
